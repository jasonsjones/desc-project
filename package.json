{
    "private": true,
    "name": "desc",
    "workspaces": [
        "packages/api"
    ],
    "scripts": {
        "lint": "lerna run lint --stream --scope @desc/api",
        "dev:api": "lerna run dev --stream --scope @desc/api",
        "test:api": "lerna run test:unit --stream --scope @desc/api"
    },
    "license": "MIT",
    "devDependencies": {
        "lerna": "^3.16.1"
    }
}
